import { useEffect, useRef } from 'react';
export default function ProjectModal({detail,onClose}) { const ref=useRef(null); useEffect(()=>{if(detail&&!ref.current.open)ref.current.showModal();},[detail]); return <dialog ref={ref} id="project-modal" onClose={onClose} onClick={e=>{if(e.target===ref.current)ref.current.close()}}><button className="modal-close" aria-label="Close project details" onClick={()=>ref.current.close()}>×</button>{detail&&<div><p className="modal-eyebrow">{detail.eyebrow}</p><h2 className="modal-title">{detail.title}</h2><div className="modal-content">{detail.body}</div></div>}</dialog>; }
