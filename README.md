# Jennifer Jiang Portfolio

React + Vite 版个人作品集。

## 本地开发

```bash
npm install
npm run dev
```

## 生产构建

```bash
npm run build
```

构建结果位于 `dist/`。部署到腾讯云 COS 时，将 `dist/` 内的全部内容上传至存储桶根目录，并将 `index.html` 设为默认首页。
